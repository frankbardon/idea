<h1><%=h @piece.title %></h1>

<p>
  <%=h @piece.description %>
</p>

<p>
  <small>posted by <%= link_to @piece.user.username, @piece.user %></small>
</p>

<%= link_to 'Edit', edit_idea_piece_path(@idea, @piece) %> |
<%= link_to 'Back', idea_path(@idea) %>